name: Daily Market Analysis

on:
  schedule:
    - cron: '15 14 * * 1-5'
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['src/**', 'requirements.txt']

env:
  OUTPUT_PATH: ${{ github.workspace }}/output

jobs:
  analyze-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # ✅ 必须允许写入
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 需要完整历史才能推送

      - name: 配置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      - name: 安装系统字体
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends fonts-wqy-microhei fonts-wqy-zenhei fonts-noto-cjk fonts-arphic-ukai fonts-arphic-uming
          sudo fc-cache -fv
          fc-list :lang=zh | head -3

      - name: 下载并配置SimHei字体
        run: |
          wget -O simhei.ttf "https://github.com/StellarCN/scp_zh/raw/master/fonts/SimHei.ttf"
          mkdir -p ~/.local/share/fonts
          cp simhei.ttf ~/.local/share/fonts/
          cp simhei.ttf ./src/
          fc-cache -fv
          fc-list | grep -i simhei || echo "SimHei字体未安装"

      - name: 安装Python依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 创建输出目录
        run: mkdir -p ${{ env.OUTPUT_PATH }}

      - name: 运行市场分析
        working-directory: ./src
        env:
          REPORT_OUTPUT_DIR: ${{ env.OUTPUT_PATH }}
        run: python main.py

      - name: 验证输出目录内容
        run: |
          echo "=== 验证文件是否存在 ==="
          ls -lh ./output/
          echo "文件数量: $(find ./output -type f | wc -l)"

      # ✅ 替换原来的 peaceiris/actions-gh-pages 步骤
      - name: 配置 Git 身份
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'

      - name: 提交生成的文件到 main 分支
        run: |
          # 只添加 output 目录
          git add output/
          
          # 检查是否有变更
          if git diff --staged --quiet; then
            echo "✅ 没有新文件需要提交"
            exit 0
          fi
          
          # 提交并推送
          git commit -m "🤖 自动生成市场分析报告 $(date '+%Y-%m-%d %H:%M')"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 摘要输出
        run: |
          echo "## 📊 市场分析完成" >> $GITHUB_STEP_SUMMARY
          echo "- **执行时间**: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "- **提交分支**: main" >> $GITHUB_STEP_SUMMARY
