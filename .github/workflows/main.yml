name: Daily Market Analysis

on:
  schedule:
    # 北京时间每天22:15（UTC 14:15）
    - cron: '15 14 * * 1-5'
  
  workflow_dispatch:
    inputs:
      analysis_type:
        description: '分析类型'
        required: false
        default: 'full'
        type: choice
        options:
          - full
          - quick
  
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'requirements.txt'

jobs:
  analyze-and-deploy:
    runs-on: ubuntu-latest
    
    # 设置权限
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 配置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      - name: 安装系统字体
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            fonts-wqy-microhei \
            fonts-wqy-zenhei \
            fonts-noto-cjk \
            fonts-arphic-ukai \
            fonts-arphic-uming
          sudo fc-cache -fv
          
          # 验证字体安装
          echo "已安装的中文字体:"
          fc-list :lang=zh | head -3
          
      - name: 下载SimHei字体文件
        run: |
          wget -O simhei.ttf "https://github.com/StellarCN/scp_zh/raw/master/fonts/SimHei.ttf"
          # 复制字体到多个位置，确保脚本能找到
          mkdir -p ~/.local/share/fonts
          cp simhei.ttf ~/.local/share/fonts/
          # 复制到src目录，确保运行时能找到
          cp simhei.ttf ./src/
          # 刷新字体缓存
          fc-cache -fv
          # 验证SimHei字体是否可用
          echo "SimHei字体状态:"
          fc-list | grep -i simhei || echo "SimHei字体未安装成功"
          # 列出所有字体位置，用于调试
          echo "字体文件位置:"
          ls -la simhei.ttf || echo "根目录无simhei.ttf"
          ls -la ./src/simhei.ttf || echo "src目录无simhei.ttf"
          ls -la ~/.local/share/fonts/simhei.ttf || echo "用户字体目录无simhei.ttf"

      - name: 安装Python依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 创建输出目录
        run: mkdir -p output

      - name: 运行字体修复测试
        working-directory: ./src
        run: python test_github_actions_font.py
      
      - name: 运行市场分析
        working-directory: ./src
        run: python main.py

      - name: 检查output目录内容
        run: |
          echo "=== 检查output目录内容 ==="
          echo "当前工作目录: $(pwd)"
          echo "output目录是否存在: $(ls -la | grep output || echo '不存在')"
          if [ -d "output" ]; then
            echo "output目录内容:"
            ls -la output/
            echo "output目录文件数量: $(ls -la output/ | wc -l)"
          else
            echo "output目录不存在，检查src/output目录:"
            ls -la src/output/ 2>/dev/null || echo "src/output目录也不存在"
          fi

      - name: 上传分析报告到Artifacts
        uses: actions/upload-artifact@v4  # 🔧 升级到v4
        with:
          name: market-analysis-report-${{ github.run_number }}
          path: |
            output/
            !output/*.log
          retention-days: 30

      # GitHub Pages 部署部分（二选一）

      # 方案1: 使用官方 deploy-pages (推荐)
      - name: 配置GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'schedule'
        uses: actions/configure-pages@v3

      - name: 部署到GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'schedule'
        uses: actions/deploy-pages@v2
        with:
          artifact_name: market-analysis-report-${{ github.run_number }}

      # 方案2: 使用 peaceiris/actions-gh-pages
      - name: 部署到GitHub Pages (备选)
        if: github.ref == 'refs/heads/main' && github.event_name == 'schedule'
        uses: peaceiris/actions-gh-pages@v4  # 🔧 升级到v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./output
          destination_dir: reports

      - name: 摘要输出
        run: |
          echo "## 📊 市场分析完成" >> $GITHUB_STEP_SUMMARY
          echo "- **执行时间**: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "- **报告链接**: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/reports/" >> $GITHUB_STEP_SUMMARY
